{# section_image #}
<section class="section_image bg-half">
    <div class="container-fluid section-padding">
        <div class="row">
            <div class="col-12 col-md-8 offset-md-2 text-center mb-5">
                <img src="{{ Image(section.image.id).src}}" alt="" class="img-fluid ">  
            </div>
        </div>
    </div>
</section>
{# end section_image #}
