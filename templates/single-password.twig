{% extends "base.twig" %}

{% block content %}
<div class="page_archive contained py-3">
	<div class="container-fluid">
		<div class="row d-flex justify-content-center ">
			<div class="col-12 col-md-6 py-5">
				<form class="password-form contact-form-style-1" action="{{site.site_url}}/wp-login.php?action=postpass" method="post">
					<label for="pwbox-{{post.ID}}">{{__('Inserisci password:', 'grit_theme')}}</label>
					<div class="d-flex"> 
						<input class="password-box" name="post_password" id="pwbox-{{post.ID}}" type="password" placeholder="Password" size="10" maxlength="10" />
						<input class="password-btn mx-2 btn btn-accent" type="submit" name="{{__('Accedi', 'grit_theme')}}" value="{{__('Accedi', 'grit_theme')}}" />
					</div>
				</form>
			</div> 
		</div> 
	</div>
</div>
	
{% endblock %}
